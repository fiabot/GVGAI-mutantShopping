BasicGame square_size=25
    SpriteSet
    	background > Immovable img=oryx/space1 hidden=True
    	base    > Immovable    color=WHITE img=oryx/planet
    	avatar > MissileAvatar img=oryx/spaceship1

    LevelMapping
    	. > background
    	0 > background base
    	A > background avatar
    	w > background

    InteractionSet
    	wall EOS > killIfFromAbove stype=wall
    	avatar EOS > collectResource geq=166

    TerminationSet
    	Timeout win=True limit=963
    	MultiSpriteCounter win=False limit=176
